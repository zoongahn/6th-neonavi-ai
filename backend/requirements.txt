Django>=5.0,<5.2
djangorestframework>=3.15
django-cors-headers>=4.4
requests>=2.32
# PostgreSQL — DATABASE_URL 을 주면 쓴다. 로컬(sqlite)에서는 필요 없다.
# 서버리스는 sqlite 가 영속되지 않아 주행 기록이 남지 않는다.
psycopg2-binary>=2.9

# ── AI 추천 실행에 필요 — ai/requirements.txt 와 동일 계열 ──
# 추천 뷰가 ai 모듈을 직접 import 하므로 백엔드 환경에도 있어야 한다.
# 권장: 이미 이것들이 설치된 neonavi/.venv 로 manage.py 를 실행한다.
pyshp>=2.3          # 노드링크 shapefile (road_type·speed_limit)
shapely>=2.0        # 공간 연산(STRtree)
pyproj>=3.6         # 좌표계 변환 (WGS84 ↔ EPSG:5186)

# ── torch 는 서빙에 필요하지 않다 ──
# 모델은 파라미터 1,062개뿐이라 순수 파이썬으로 계산한다(model_a 의 pure 백엔드).
# torch 를 넣으면 733MB 라 Vercel 서버리스 250MB 한도를 혼자 넘긴다.
# 설치돼 있으면 자동으로 torch 를 쓰고, 없으면 ai/data/model_weights.json 으로
# 떨어진다(NEONAVI_ENGINE=torch|pure|auto 로 고정 가능).
# 재학습 후에는 `python -m ai.export_weights` 로 그 JSON 을 다시 뽑을 것.
#   torch>=2.2      # 학습용. ai/requirements.txt 참고
# pyarrow 는 서빙 경로에서 import 되지 않는다(학습 데이터 포맷 전용).
#   pyarrow>=15.0

# ── XAI 설명 생성(LLM) ──
# services.explain_route_detail 이 ai.xai_llm 을 import 하고, 그 모듈이
# 모듈 최상위에서 gradio_client 를 부른다. 없으면 routes 앱 자체가
# import 에 실패해 /api/routes/* 전부가 죽는다.
gradio_client>=1.0
